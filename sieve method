n=int(input())
prime_array=[1]*n
for ind in range(2,int(n**0.5)+1):
    if(prime_array[ind]==1):
        for i in range(ind*ind,n,ind):
            prime_array[i]=0
prime_count=0
for i in range(2,n):
    if(prime_array[i]==1):
        prime_count+=1
print(prime_count)
